These tests are made for NVIDIA Jetson AGX Orin

The Makefile will compile 'libgpiod.c' to 'gpio' binary which can be used to test gpio pins.
'sysfs.sh' is a bash script that tests gpio pins by writing to sysfs
